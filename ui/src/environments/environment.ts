export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3003',
  /** Mirror `UI_SHOW_LIFETIME_PRICING` in `api/.env` for local parity; set true to develop/test the Lifetime tier on /pricing. */
  showLifetimeOnPricing: true,
};
