/**
 * Plaintext server `searchable_words` cap (create/edit forms, import slice, client validation).
 * Same value as {@link VaultContractValidator::SEARCHABLE_WORDS_MAX_BYTES} in the API (PHP `strlen` after trim).
 */
export const VAULT_SEARCHABLE_WORDS_MAX_LENGTH = 64;
