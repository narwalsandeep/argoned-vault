; Loaded after image defaults — tune pool for API concurrency (adjust max_children to RAM).

[www]
pm = dynamic
pm.max_children = 30
pm.start_servers = 4
pm.min_spare_servers = 4
pm.max_spare_servers = 12
pm.max_requests = 500
